create table netflixUserbase(User_ID int(30),
	Subscription_Type varchar(30),	
    Monthly_Revenue	int(10),
    Join_Date varchar(30),	
    Last_Payment_Date varchar(30),
    Country varchar(30),
	Age int(3),
	Gender varchar(10),
    Device	varchar(20),
    Plan_Duration varchar(20));

select * from netflixuserbase;
